{
  "schema_version": 1,
  "hook_timeout_seconds": 5,
  "cases": [
    {
      "id": "deny_push_to_main",
      "input": "stdin/pre_tool_use_shell_minimal.json",
      "config": "config/enforcing.toml",
      "expected_stdout_utf8": "{\"decision\":\"deny\",\"reason\":\"Push to protected branch (main/master)\"}\n",
      "expected_exit_code": 2,
      "expected_harness_behavior": "blocked"
    },
    {
      "id": "allow_git_status",
      "input": "stdin/pre_tool_use_shell_allow.json",
      "config": "config/enforcing.toml",
      "expected_stdout_utf8": "{\"decision\":\"allow\"}\n",
      "expected_exit_code": 0,
      "expected_harness_behavior": "proceeds_allowed"
    },
    {
      "id": "abstain_unmatched",
      "input": "stdin/pre_tool_use_shell_abstain.json",
      "config": "config/enforcing.toml",
      "expected_stdout_utf8": "",
      "expected_exit_code": 1,
      "expected_harness_behavior": "defers_fail_open"
    }
  ]
}
